# SQLAlchemy-style URL pointing at the same Postgres the Node backend uses.
# Use "postgres" as the host when running inside docker-compose, "localhost" when running ml-service directly.
DATABASE_URL=postgresql+psycopg2://transitops:transitops@localhost:5432/transitops
PORT=8000
