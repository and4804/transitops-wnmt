name: FE-1 Component Tests — Auth, Vehicles, Trips, Maintenance, Dashboard

on:
  pull_request:
    paths:
      - 'apps/fleet-manager-ui/**'
      - 'contracts/fe1/**'
  push:
    branches: [main]

env:
  SPECMATIC_VERSION: '2.34.1'
  STUB_URL: http://localhost:9000
  CONTRACT_REPO_URL: https://github.com/transitops/central-contract-repo.git

jobs:
  component-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: apps/fleet-manager-ui/package-lock.json

      - name: Install dependencies
        working-directory: apps/fleet-manager-ui
        run: npm ci

      - name: Run unit tests
        working-directory: apps/fleet-manager-ui
        run: npm run test:unit

      - name: Set up JDK (Specmatic runtime)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Download Specmatic
        run: |
          curl -L -o specmatic.jar \
            "https://github.com/specmatic/specmatic/releases/download/${SPECMATIC_VERSION}/specmatic.jar"

      - name: Start Specmatic stub (both contracts)
        working-directory: contracts/fe1
        run: |
          java -jar ../../specmatic.jar stub --config specmatic.json --port 9000 &
          npx wait-on ${STUB_URL}/vehicles --timeout 30000

      - name: Run component tests against the stub
        working-directory: apps/fleet-manager-ui
        env:
          API_BASE_URL: ${{ env.STUB_URL }}
        run: npm run test:component

      - name: Upload Specmatic stub logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fe1-stub-logs
          path: contracts/fe1/logs/
