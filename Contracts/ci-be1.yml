name: BE-1 Contract Tests — Fleet & Trip Operations

on:
  pull_request:
    paths:
      - 'services/fleet-trips/**'
      - 'contracts/be1/**'
  push:
    branches: [main]

env:
  SPECMATIC_VERSION: '2.34.1'
  PROVIDER_URL: http://localhost:8081
  CONTRACT_REPO_URL: https://github.com/transitops/central-contract-repo.git

jobs:
  contract-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: services/fleet-trips/package-lock.json

      - name: Install dependencies
        working-directory: services/fleet-trips
        run: npm ci

      - name: Run unit tests
        working-directory: services/fleet-trips
        run: npm run test:unit

      - name: Set up JDK (Specmatic runtime)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Download Specmatic
        run: |
          curl -L -o specmatic.jar \
            "https://github.com/specmatic/specmatic/releases/download/${SPECMATIC_VERSION}/specmatic.jar"

      - name: Start provider app
        working-directory: services/fleet-trips
        run: |
          npm run build
          npm start &
          npx wait-on ${PROVIDER_URL}/health --timeout 30000

      - name: Run Specmatic contract tests
        working-directory: contracts/be1
        run: java -jar ../../specmatic.jar test --config specmatic.json

      - name: Upload Specmatic report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: be1-specmatic-report
          path: contracts/be1/build/reports/specmatic/

      - name: Run component tests (only after contract tests pass)
        if: success()
        working-directory: services/fleet-trips
        run: npm run test:component
